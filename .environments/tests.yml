# Conda environment for unit testing
name: narupatools-test
channels:
  - conda-forge
dependencies:
# Testing Framework.
  - pytest=6.*
  - pytest-xdist=2.*
  - pytest-timeout=1.4.*
  - hypothesis=6.*
